import { ContentPage } from "@/components/content-page";

export default function FaqPage() {
  return (
    <ContentPage
      title="FAQ"
      body="Add sizing, production, shipping, print care, and checkout questions before launch."
    />
  );
}
